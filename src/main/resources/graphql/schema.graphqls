type Book {
    id: ID!
    title: String!
    author: String!
}

type Query {
    getAllBooks: [Book]
    getBookById(id: ID!): Book
}

type Mutation {
    addBook(title: String!, author: String!): Book
}
